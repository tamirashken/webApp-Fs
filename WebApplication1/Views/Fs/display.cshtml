
@{
    ViewBag.Title = "display";
}
<!--
<body>
    <canvas id="myCanvas" style="border:9px solid black;"></canvas>
    <script>
        var c = document.getElementById("myCanvas");
        c.width = document.documentElement.clientWidth;
        c.height = document.documentElement.clientHeight;

        var lon = Session["lon"];
        var lonNorm = (lon + 180) * (c.width / 360);
        var lat = Session["lat"];
        var lonNorm = (lat + 180) * (c.heigt / 360);
        var ctx = c.getContext("2d");
        ctx.beginPath();
        ctx.arc(300, 300, 90, 0, 2 * Math.PI);
        ctx.lineWidth = 20;
        ctx.lineStyle = 'black';
        ctx.fillStyle = 'red';
        ctx.fill();
        ctx.stroke();

    </script>
</body>
    -->


<link rel="stylesheet" type="text/css" href="~/Style/circle.css">
<head>
    <script>
        //var dimension = [document.documentElement.clientWidth, document.documentElement.clientHeight];
        var draw = document.getElementById("circle");
        var lon = Session["lon"];
        var lat = Session["lat"];
        changeColors(lon, lat);

        function changeColors() {
            var lonNorm = (lon + 180) * (screen.height / 360);
            var latNorm = (lat + 90) * (screen.width / 180);
            //draw.style.left = lonNorm
            draw.style.backgroundColor = "blue";
            //draw.style.top = latNorm
        }
    </script>
</head>
<body>

    <div id="circle"> </div>

</body>
<!--var canvas = document.getElementById("myCanvas");
var ctx = canvas.getContext("2d");
ctx.strokeStyle = "red";
ctx.strokeRect(75, 25, 50, 50);    -->
